services:
  backend:
    build: .
    ports:
      - "8000:8000"
    image: mohithailuri/chess-backend:${IMAGE_TAG}
    environment:
      - PORT=8000
  
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    image: mohithailuri/chess-frontend:${IMAGE_TAG}
